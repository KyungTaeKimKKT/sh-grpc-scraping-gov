apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scraping-gov.fullname" . }}-test-connection"
  labels:
    {{- include "scraping-gov.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scraping-gov.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
